apiVersion: v1
kind: Service
metadata:
  name: {{ include "vision-agent.fullname" . }}
  labels:
    {{- include "vision-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vision-agent.selectorLabels" . | nindent 4 }}
