[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "vision-agents-plugins-inworld"
dynamic = ["version"]
description = "Inworld AI integration for Vision Agents (LLM/VLM router, TTS, Realtime WebRTC)"
readme = "README.md"
keywords = ["inworld", "TTS", "text-to-speech", "realtime", "webrtc", "router", "LLM", "VLM", "AI", "voice agents", "agents"]
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "vision-agents",
    "vision-agents-plugins-openai",
    "httpx>=0.28.1",
    "websockets>=14.0",
    "aiortc>=1.9",
    "openai[realtime]>=2.26.0,<3",
]

[project.urls]
Documentation = "https://visionagents.ai/"
Website = "https://visionagents.ai/"
Source = "https://github.com/GetStream/Vision-Agents"

[tool.hatch.version]
source = "vcs"
raw-options = { root = "..", search_parent_directories = true, fallback_version = "0.0.0" }

[tool.hatch.build.targets.wheel]
packages = ["vision_agents"]

[tool.hatch.build.targets.sdist]
include = ["/vision_agents"]

[tool.uv.sources]
vision-agents = { workspace = true }
vision-agents-plugins-openai = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.0.0",
]
